# dependencies (Yarn workspace — hoisted to root node_modules/)
node_modules/

# build output
app/dist/
server/target/
app/h5/.nuxt/
app/h5/.output/
app/h5/.data/

# logs and temp files
*.log
*.err
run.log
run.err
test_output.txt

# local automation/browser traces
.playwright-cli/

# test coverage reports (generated artifacts)
test/reports/

# local env overrides (never commit real values)
.env
.env.local

# OS/editor noise
.DS_Store
Thumbs.db

CLAUDE.md
local/
local/seed-data.sql
.claude/

# SonarQube scanner working directory (generated during local analysis)
.scannerwork/
sonar-scanner.zip
sonar-scanner*/

# Runtime upload directory and test output artifacts
server/uploads/
test-results/
