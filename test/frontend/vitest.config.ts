/**
 * 此文件仅作说明用途。
 *
 * 前端单元测试的实际 Vitest 配置位于 app/frontend/vitest.config.ts，
 * 该配置已包含 test/frontend/** 的扫描路径。
 *
 * 运行命令：
 *   cd app/frontend && npx vitest run
 *   # 或通过 run-all.bat / run-all.sh 一键执行全套测试
 */
export {}
