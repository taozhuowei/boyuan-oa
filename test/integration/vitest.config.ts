/**
 * 此文件仅作说明用途。
 *
 * 集成测试的实际 Vitest 配置位于 app/frontend/vitest.integration.config.ts，
 * 该配置已包含 test/integration/** 的扫描路径（Yarn workspace node_modules 限制）。
 *
 * 运行命令：
 *   cd app/frontend && npx vitest run --config vitest.integration.config.ts
 *   # 或通过 run-all.bat / run-all.sh 一键执行全套测试
 */
export {}
